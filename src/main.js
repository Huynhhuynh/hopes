/**
 * Hopes main javascript
 * 
 * @version 1.0.0
 * @package Hopes
 */

import './scss/main.scss';
import './donation-form';

;( ( w, $ ) => {
  'use strict';

  const Ready = () => {

  }

  /**
   * DOM Ready
   */
  $( Ready );

} )( window, jQuery )