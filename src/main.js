/**
 * Hopes main javascript
 * 
 * @version 1.0.0
 * @package Hopes
 */

import './scss/main.scss'

;( ( w, $ ) => {
  'use strict'

  const Ready = () => {

  }

  /**
   * DOM Ready
   */
  $( Ready )

} )( window, jQuery )